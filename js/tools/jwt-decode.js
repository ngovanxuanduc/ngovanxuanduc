(function(){var inn=document.getElementById("jwt-in");var header=document.getElementById("jwt-header");var payload=document.getElementById("jwt-payload");var meta=document.getElementById("jwt-meta");var btnRun=document.getElementById("jwt-run");var btnCopy=document.getElementById("jwt-copy");var btnClear=document.getElementById("jwt-clear");if(!inn)return;function setMeta(msg){if(meta)meta.textContent=msg||"";}function b64urlToJson(part){if(part==null||part==="")throw new Error("Phần JWT trống");var s=String(part).replace(/-/g,"+").replace(/_/g,"/");while(s.length%4)s+="=";var bin;try{bin=atob(s);}catch(e){throw new Error("Base64URL không hợp lệ");}var bytes=new Uint8Array(bin.length);for(var i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);var text=new TextDecoder().decode(bytes);return JSON.stringify(JSON.parse(text),null,2);}function run(){try{var parts=String(inn.value||"").trim().split(".");if(parts.length<2)throw new Error("JWT cần ≥ 2 phần (header.payload)");if(header)header.value=b64urlToJson(parts[0]);if(payload)payload.value=b64urlToJson(parts[1]);setMeta(parts.length===3?"Decoded (signature không verify)":"Decoded · "+parts.length+" parts");}catch(e){setMeta("Lỗi: "+(e&&e.message?e.message:e));if(header)header.value="";if(payload)payload.value="";}}if(btnRun)btnRun.addEventListener("click",run);if(btnCopy){if(!btnCopy.getAttribute("data-label"))btnCopy.setAttribute("data-label","Copy");btnCopy.addEventListener("click",function(){var text=(payload&&payload.value)||"";if(window.ToolLib)ToolLib.copyText(text,btnCopy);else if(navigator.clipboard)navigator.clipboard.writeText(text||"");});}if(btnClear)btnClear.addEventListener("click",function(){inn.value="";if(header)header.value="";if(payload)payload.value="";setMeta("");});})();