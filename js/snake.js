(function(){var canvas=document.getElementById("snake-canvas");var statusEl=document.getElementById("snake-status");var scoreEl=document.getElementById("snake-score");var speedLabelEl=document.getElementById("snake-speed-label");var startBtn=document.getElementById("snake-start");var resetBtn=document.getElementById("snake-reset");if(!canvas)return;var ctx=canvas.getContext("2d");var GRID=16;var CELL=18;canvas.width=GRID*CELL;canvas.height=GRID*CELL;var SPEED_MS={slow:220,normal:150,fast:95,};var SPEED_LABEL={slow:"Chậm",normal:"Vừa",fast:"Nhanh",};var SPEED_KEY="game-snake-speed";var speed="normal";try{var saved=localStorage.getItem(SPEED_KEY);if(saved&&SPEED_MS[saved])speed=saved;}catch(e){}var snake,dir,nextDir,food,score,tick,running,dead;function setStatus(text,cls){if(!statusEl)return;statusEl.className="game-status"+(cls?" "+cls:"");statusEl.textContent=text;}function updateScore(){if(scoreEl)scoreEl.textContent=String(score);}function updateSpeedUi(){if(speedLabelEl)speedLabelEl.textContent=SPEED_LABEL[speed]||speed;document.querySelectorAll("[data-snake-speed]").forEach(function(btn){var on=btn.getAttribute("data-snake-speed")===speed;btn.classList.toggle("is-active",on);btn.setAttribute("aria-pressed",on?"true":"false");});}function intervalMs(){return SPEED_MS[speed]||SPEED_MS.normal;}function randCell(){return{x:Math.floor(Math.random()*GRID),y:Math.floor(Math.random()*GRID),};}function placeFood(){var p;do{p=randCell();}while(snake.some(function(s){return s.x===p.x&&s.y===p.y;}));food=p;}function stopTick(){if(tick){clearInterval(tick);tick=null;}}function restartTick(){stopTick();if(running&&!dead){tick=setInterval(step,intervalMs());}}function reset(){stopTick();running=false;dead=false;snake=[{x:8,y:8},{x:7,y:8},{x:6,y:8},];dir={x:1,y:0};nextDir={x:1,y:0};score=0;updateScore();updateSpeedUi();placeFood();setStatus("Nhấn Bắt đầu hoặc phím mũi tên / WASD.");draw();}function drawCell(x,y,color){ctx.fillStyle=color;ctx.fillRect(x*CELL+1,y*CELL+1,CELL-2,CELL-2);}function draw(){ctx.fillStyle="#0a0a0b";ctx.fillRect(0,0,canvas.width,canvas.height);ctx.strokeStyle="#16161a";ctx.lineWidth=1;for(var i=0;i<=GRID;i++){ctx.beginPath();ctx.moveTo(i*CELL,0);ctx.lineTo(i*CELL,GRID*CELL);ctx.stroke();ctx.beginPath();ctx.moveTo(0,i*CELL);ctx.lineTo(GRID*CELL,i*CELL);ctx.stroke();}drawCell(food.x,food.y,"#a78bfa");snake.forEach(function(s,i){drawCell(s.x,s.y,i===0?"#c4b5fd":"#6d5a9e");});}function step(){dir=nextDir;var head={x:snake[0].x+dir.x,y:snake[0].y+dir.y,};if(head.x<0||head.y<0||head.x>=GRID||head.y>=GRID||snake.some(function(s){return s.x===head.x&&s.y===head.y;})){dead=true;running=false;stopTick();setStatus("Game over — điểm "+score+".","is-draw");draw();return;}snake.unshift(head);if(head.x===food.x&&head.y===food.y){score+=1;updateScore();placeFood();setStatus("Điểm: "+score);}else{snake.pop();}draw();}function start(){if(running)return;if(dead)reset();running=true;setStatus("Đang chơi… ("+(SPEED_LABEL[speed]||speed)+")");tick=setInterval(step,intervalMs());}function setSpeed(next){if(!SPEED_MS[next]||next===speed)return;speed=next;try{localStorage.setItem(SPEED_KEY,speed);}catch(e){}updateSpeedUi();if(running&&!dead){restartTick();setStatus("Đang chơi… ("+SPEED_LABEL[speed]+")");}else if(!dead){setStatus("Tốc độ: "+SPEED_LABEL[speed]+" · Nhấn Bắt đầu");}}function setDir(nx,ny){if(dir.x+nx===0&&dir.y+ny===0)return;nextDir={x:nx,y:ny};if(!running&&!dead)start();}document.addEventListener("keydown",function(e){var k=e.key.toLowerCase();if(["arrowup","arrowdown","arrowleft","arrowright","w","a","s","d"].indexOf(k)!==-1){e.preventDefault();}if(k==="arrowup"||k==="w")setDir(0,-1);if(k==="arrowdown"||k==="s")setDir(0,1);if(k==="arrowleft"||k==="a")setDir(-1,0);if(k==="arrowright"||k==="d")setDir(1,0);});document.querySelectorAll("[data-snake-dir]").forEach(function(btn){btn.addEventListener("click",function(){var d=btn.dataset.snakeDir;if(d==="up")setDir(0,-1);if(d==="down")setDir(0,1);if(d==="left")setDir(-1,0);if(d==="right")setDir(1,0);});});document.querySelectorAll("[data-snake-speed]").forEach(function(btn){btn.addEventListener("click",function(){setSpeed(btn.getAttribute("data-snake-speed"));});});if(startBtn)startBtn.addEventListener("click",start);if(resetBtn)resetBtn.addEventListener("click",reset);updateSpeedUi();reset();})();